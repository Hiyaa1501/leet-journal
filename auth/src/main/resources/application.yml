server:
  port: 8000

spring:
  application:
    name: auth
  datasource:
    url: jdbc:postgresql://localhost:5432/db?options=-c%20timezone=Asia/Kolkata
    username: user
    password: pass
  sql:
    init:
      mode: ALWAYS

logging:
  level:
    org.springframework.security: TRACE